const commonParameters = () => [
  {
    name    : 'priority',
    summary : 'Designates the priority (order) of the badge on the README.md page.',
    matcher : /^\d+/
  }
]

export { commonParameters }
